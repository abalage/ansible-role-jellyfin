#!/bin/bash
JELLYFINDIR="{{ jellyfin_home }}"
FFMPEGDIR="{{ ffmpeg_dir }}"

$JELLYFINDIR/jellyfin/jellyfin \
 -d $JELLYFINDIR/data \
 -C $JELLYFINDIR/cache \
 -c $JELLYFINDIR/config \
 -l $JELLYFINDIR/log \
 --ffmpeg $FFMPEGDIR/ffmpeg